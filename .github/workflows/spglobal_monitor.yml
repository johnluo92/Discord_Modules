name: S&P Global Press Room Monitor

on:
  schedule:
    - cron: '*/30 13-22 * * 1-5'   # Monday–Friday every 30 min, 9 AM–6 PM ET (13:00–22:00 UTC)
  workflow_dispatch:
    inputs:
      mode:
        description: 'Run mode'
        required: false
        default: 'normal'
        type: choice
        options:
          - normal
          - test

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        working-directory: ./Index_reconstitution
        run: pip install -r requirements.txt

      - name: Run monitor
        working-directory: ./Index_reconstitution
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          MODE="${{ github.event.inputs.mode || 'normal' }}"
          if [ "$MODE" = "test" ]; then
            python3 spglobal_tracker.py --test
          else
            python3 spglobal_tracker.py
          fi

      - name: Commit updated state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Index_reconstitution/spglobal_state.json
          git diff --cached --quiet || git commit -m "chore: update spglobal state [skip ci]"
          git pull --rebase
          git push
